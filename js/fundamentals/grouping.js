// template literals

const name = "Rafael";
const age = 14;

console.log(`Meu nome é ${name} e tenho ${age} anos`);

// +

console.log("Meu nome é " + name + " e tenho " + age + " anos");

// criando mais de uma variável por vez

let isRaining, season;

isRaining = true;
season = "Winter";

console.log(isRaining, season);
